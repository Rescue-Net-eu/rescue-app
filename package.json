{
  "name": "rescue-net",
  "packageManager": "npm@10.8.2",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^1.5.3"
  }
}
