import React from 'react';

export default function VolunteerProfile({ volunteerId }) {
  return (
    <div className="container mx-auto py-8">
      {/* TODO: fetch /volunteers/:volunteerId, show header, bio, recent missions, send message button */}
    </div>
  );
}
