import React from 'react';

export default function AlertDetail({ alertId }) {
  return (
    <div className="container mx-auto py-8">
      {/* TODO: fetch /alerts/:alertId, show map inset, photo carousel, description */}
    </div>
  );
}
